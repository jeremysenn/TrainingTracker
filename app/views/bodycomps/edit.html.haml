- title "Edit Body Comp"
#client_notice.round
  = link_to @client.full_name, client_path(@client), :title => "Client information", :class => "tool-tip"

= render 'form'

%p
  = link_to "Cancel", client_path(@client) + "#clientbodycomps_tab", :title => "Back", :class => "tool-tip"
  =# link_to "Show", bodycomp_path(@bodycomp)
  =# link_to "View All", bodycomps_path
