- if current_user.is_client?
  #client_notice.round
    = @client.full_name
=# link_to image_tag("orange_plus_icon.png", :alt => "New"), new_exercise_path, :title => 'Add new exercise', :class => 'tool-tip'
= link_to "New Exercise", new_exercise_path, :title => 'Add new exercise', :class => 'tool-tip'
%br
%br

- unless @exercises.empty?
  =# will_paginate(@exercises)
  - for exercise in @exercises
    %p= link_to exercise.name, exercise_path(exercise), :title => "Show exercise", :class => "tool-tip"

  
  -# for exercise in @exercises
    =# link_to exercise.name, exercise_path(exercise), :title => "Show exercise", :class => "tool-tip"
    =# link_to image_tag("EditIcon.jpg"), edit_exercise_path(exercise), :title => "Edit exercise", :class => "tool-tip"
    =# exercise.highest_weight
    =# exercise.highest_set.reps.to_s + " x " + exercise.highest_set.weight.to_s if exercise.highest_set
    =# link_to 'Show', exercise
    =# link_to 'Edit', edit_exercise_path(exercise)
    =# link_to 'Delete', exercise, :confirm => 'Are you sure?', :method => :delete

  =# will_paginate(@exercises)
